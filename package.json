{
  "name": "apim-delegation-functions",
  "version": "1.0.0",
  "description": "APIM Developer Portal delegation with Okta OIDC",
  "scripts": {
    "start": "func start",
    "dev": "func start --verbose",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint . --ext .js",
    "lint:fix": "eslint . --ext .js --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "build": "echo 'No build step required for Node.js Azure Functions'",
    "deploy:dev": "./scripts/deploy.sh dev",
    "deploy:staging": "./scripts/deploy.sh staging",
    "deploy:prod": "./scripts/deploy.sh prod",
    "setup": "cp local.settings.template.json local.settings.json",
    "clean": "rm -rf node_modules coverage .nyc_output",
    "validate": "npm run lint && npm run format:check && npm run test"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-node": "^11.1.0",
    "jest": "^29.7.0",
    "prettier": "^3.2.5",
    "supertest": "^6.3.4"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "keywords": [
    "azure",
    "functions",
    "apim",
    "delegation",
    "okta",
    "oidc",
    "authentication"
  ],
  "author": "",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": ""
  },
  "bugs": {
    "url": ""
  },
  "homepage": ""
}
